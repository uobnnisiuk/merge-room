Start the development server for merge-room.

Run the following commands:
1. `cd web && pnpm dev`

The frontend will be available at http://localhost:5173
The backend API will be available at http://localhost:3001

If there are dependency issues, run `pnpm install` first.
If there are SQLite native module issues, run `pnpm sqlite:rebuild`.
